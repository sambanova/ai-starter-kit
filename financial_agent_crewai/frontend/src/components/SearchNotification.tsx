interface ISearchNotification {
  show: boolean;
  time: string;
  resultCount: number;
}

const SearchNotification = ({
  show,
  time,
  resultCount,
}: ISearchNotification) => {
  return <></>;
};

export default SearchNotification;
