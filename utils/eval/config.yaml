eval_dataset:
  path: eval_set.csv # for CSV file
  # hf_dataset_name: squad_v2 # for HF dataset  
  question_col: question
  answer_col: ground_truth
  context_col: context

llms:
  - name: llm1 
    base_url: https://api.example.com
    project_id: abc123
    endpoint_id: xyz789 
    api_key: secret
    model_kwargs:
      temperature: 0.5

  - name: llm2
    base_url: https://api2.example.com
    project_id: abc456
    endpoint_id: xyz123
    api_key: secret2 
    model_kwargs:
      temperature: 0.3

eval_llms:
  - base_url: https://eval-api.example.com
    project_id: eval123
    endpoint_id: eval456
    api_key: eval_secret

vector_db:  
  location: adi_vdb.chromadb

embeddings:
  model_name: hkunlp/instructor-large 

evaluation:
  num_samples: 100
  log_wandb: true
  project_name: rag-pipeline-eval
  eval_name: test-run-1
  methodology: rag_pipeline

pipeline:
  class: rag_pipeline.RAGPipeline  
  kwargs:
    vector_db_location: adi_vdb.chromadb