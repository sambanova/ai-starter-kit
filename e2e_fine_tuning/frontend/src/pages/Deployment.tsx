import { Container, Typography } from "@mui/material";

const Deployment = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Welcome to the Deployment page
      </Typography>
      <Typography variant="body1">
        This is the Deployment page of the application.
      </Typography>
    </Container>
  );
};

export default Deployment;
